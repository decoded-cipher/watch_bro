{
  "name": "movie-bot",
  "version": "1.0.0",
  "description": "Telegram movie tracker bot on Cloudflare Workers",
  "private": true,
  "license": "MIT",
  "author": "Arjun Krishna",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy",
    "db:migrate:local": "bunx wrangler d1 execute cipher_movie_bot --local --file=schema.sql",
    "db:migrate:remote": "bunx wrangler d1 execute cipher_movie_bot --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.12.8"
  },
  "devDependencies": {}
}
